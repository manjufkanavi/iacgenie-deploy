all:
  children:
    servers:
      hosts:
        iacgenie-server:
          ansible_host: 192.168.0.118
          ansible_user: mkanavi
          ansible_python_interpreter: /usr/bin/python3

    docker_hosts:
      children:
        - servers

    web_frontend:
      children:
        - servers

    database_cluster:
      children:
        - servers

    message_queue:
      children:
        - servers
